/*
    DOM Traversing:

    Move up / down in the DOM tree to select expected node/element, by using some methods:

        - parentNode    
            --> in JS, we can NOT delete an HTML element. We can ONLY remove the child element 
            --> so we have to select the parent

        - 
*/